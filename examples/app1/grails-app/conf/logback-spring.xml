<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <withJansi>true</withJansi>
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>${CONSOLE_LOG_THRESHOLD}</level>
    </filter>
    <encoder>
      <pattern>${CONSOLE_LOG_PATTERN}</pattern>
      <charset>${CONSOLE_LOG_CHARSET}</charset>
    </encoder>
  </appender>

  <root level="ERROR">
    <appender-ref ref="CONSOLE"/>
  </root>

  <!-- Hide Hibernate deprecation warnings -->
  <logger name="org.hibernate.orm.deprecation" level="ERROR" additivity="false"><appender-ref ref="CONSOLE"/></logger>

  <!-- Log SQL queries -->
<!--
  <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" additivity="false"><appender-ref ref="CONSOLE"/></logger>
  <logger name="org.hibernate.SQL" level="DEBUG" additivity="false"><appender-ref ref="CONSOLE"/></logger>
-->

  <!-- Log PreQueryListener for Grails logical delete -->
<!--
  <logger name="grails.logical.delete.PreQueryListener" level="DEBUG" additivity="false"><appender-ref ref="CONSOLE"/></logger>
-->

</configuration>
